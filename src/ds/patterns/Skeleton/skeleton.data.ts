/* 01 — Appointment list loading (eniyicihaz.com) */

export const appointmentRows = [1, 2, 3];

/* 03 — Article/blog post loading, text lines */

export const articleLines = {
    titleWidth: "85%",
    paragraphLines: 4,
    lastLineWidth: "60%"
};

/* 04 — Product grid loading, rectangle + rounded */

export const productCards = [1, 2, 3, 4];

/* 05 — Chat message list loading, circle avatars + text, pulse animation */

export const chatRows = [1, 2, 3];
